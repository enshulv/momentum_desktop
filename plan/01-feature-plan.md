# 功能需求计划 - Issue #3（按代码实况更新）

## 需求来源
- Issue: #3 - 新功能请求
- 状态: 进行中（文档已按当前代码重新核对）
- 本次核对日期: 2026-02-28

---

## 需求完成状态（实况）

### 需求1：倒计时小窗及窗口置顶功能
**当前实现状态**: 已完成

- [x] 实现倒计时小窗口模式（通过主窗口切换为画中画尺寸）
- [x] 支持窗口置顶功能（`window:toggle-always-on-top`）
- [x] 小窗模式下显示倒计时信息
- [x] 支持大小窗切换（`window:enter-mini-mode` / `window:exit-mini-mode`）

**实现说明（与原方案差异）**:
- 当前不是“新建独立小窗”，而是对主窗口 `setBounds` 进入小窗模式。
- 小窗位置/尺寸/置顶状态会写入 `window-state.json` 持久化。
- 最终方案以低耦合为目标：不新增独立窗口 IPC 主链路，功能由现有窗口状态流完成。

---

### 需求2：国策树节点确定后允许再编辑
**当前实现状态**: 已完成

- [x] 允许编辑已确定的国策树节点（RSIPView 已接入编辑入口）
- [x] 编辑变更进入操作历史（通过 `UPDATE_RSIP_NODE`）
- [x] 支持修改节点核心属性后保存（`title/rule/计时/定时`）
- [x] 编辑后更新节点结构并持久化

**现状补充**:
- `RSIPNodeEditDialog.tsx` 已与 `RSIPNode` 当前模型对齐并接入 `RSIPView.tsx`。

---

### 需求3：撤回操作功能
**当前实现状态**: 已完成（生产可用范围）

- [x] 实现操作历史记录系统（`OperationHistoryManager`）
- [x] 支持 Undo/Redo（当前覆盖 CREATE/UPDATE/DELETE 链条）
- [x] 键盘快捷键支持（Ctrl/Cmd+Z，Ctrl/Cmd+Y，Ctrl/Cmd+Shift+Z）
- [x] 可配置最大历史记录数（当前实例为 50）
- [x] 历史持久化到 localStorage
- [x] 撤回/重做按钮已接入界面
- [x] 操作历史面板已接入界面（安全模式：仅在传入回调时显示恢复按钮）
- [x] 覆盖 RESTORE_CHAIN、RSIP 主流程回放

---

## 当前阶段建议

### 第一阶段（已落地）
- 小窗/置顶主流程（同窗体画中画）
- Undo/Redo 基础能力与持久化

### 第二阶段（待补全）
- 已并入第一阶段完成

### 第三阶段（收尾）
- 清理无效 IPC 通道与未使用组件
- 集成测试与文档收敛

---

## 验收口径（更新后）
- [x] RSIP 节点可编辑且可持久化
- [x] Undo/Redo 覆盖链条 + RSIP 主流程操作
- [x] Undo/Redo 控件在 UI 可见且可用
- [x] 小窗功能按既定目标完成（不引入额外复杂 IPC）
- [x] 关键路径静态检查通过（lint 无 error）
